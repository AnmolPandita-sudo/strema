import { SignUpCard } from '@/components/glass-auth';

export default function Page() {
  return (
    <main className="auth-shell">
      <div className="auth-bg auth-bg--alt" />
      <SignUpCard />
    </main>
  );
}